# Build
/target/
**/*.rs.bk

# Frontend
web/node_modules/
web/dist/

# Secrets and local env (never commit)
.env
.env.*
!.env.example
.secrets.env
*.pem
*.key

# BigSeller session cookies / tokens
.session.json
*.session.json
sessions/

# OCR model weights (large; fetch via scripts/fetch-model.sh)
models/*.onnx
!models/.gitkeep

# Infisical local overrides (workspace id is committed via .infisical.json)
.infisical.local.json

# Tooling / capture artifacts
.playwright-cli/
.playwright/
*.log
/tmp/

# Local/generated reports and source data dumps
*.pdf
*.xlsx
/logs/*
!/logs/logos-logistics/
!/logs/logos-logistics/**

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp
*~

# Agent skill copies (optional local)
.agents/
skills-lock.json
